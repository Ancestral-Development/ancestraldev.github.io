function scrollToTeam() {
  var element = document.querySelector('.mobile-android-development');
  if (element) {
    element.scrollIntoView({ behavior: 'smooth' });
  }
}

function scrollToServices() {
  var element = document.querySelector('.services');
  if (element) {
    element.scrollIntoView({ behavior: 'smooth' });
  }
}

function scrollToAbout() {
  var element = document.querySelector('.about-us');
  if (element) {
    element.scrollIntoView({ behavior: 'smooth' });
  }
}

function scrollToWork() {
  var element = document.querySelector('.our-team-is-container');
  if (element) {
    element.scrollIntoView({ behavior: 'smooth' });
  }
}